public class tenerary {
    public static void main(String[] args) {
        // int salary=20000;

        // double tax_rate=(salary>=40000) ? 0.25 : 0.15;
        // System.out.println(tax_rate);

        int num =3;
        String even_odd = (num%2 == 0) ? "Even" : "Odd";
        System.out.println(even_odd);
    }
}
